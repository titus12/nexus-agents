---
name: wf-unity-logic-mod
description: Unity logic modification workflow entry. Use for non-UI or shared Unity gameplay/client logic changes that require bounded implementation and verification. Invoke explicitly with $wf-unity-logic-mod.
---

# wf-unity-logic-mod

## Invocation

- Codex skill trigger: `$wf-unity-logic-mod`

## Workflow

Read `.claude/workflows/wf-unity-logic-mod.md` from the repository root completely and follow it as the source of truth.

Treat the user's remaining prompt as the workflow input.
