---
description: Run the design workflow
argument-hint: [workflow input]
---

# /wf-design

Read `.claude/workflows/design.md` from the repository root and follow it as the source of truth.

Treat `$ARGUMENTS` as the workflow input.
