---
description: Run the Go bugfix workflow
argument-hint: [workflow input]
---

# /wf-go-bugfix

Read `.claude/workflows/go-bugfix.md` from the repository root and follow it as the source of truth.

Treat `$ARGUMENTS` as the workflow input.
