---
description: Run the commit-gate workflow
argument-hint: [workflow input]
---

# /wf-commit

Read `.claude/workflows/commit-gate.md` from the repository root and follow it as the source of truth.

Treat `$ARGUMENTS` as the workflow input.
