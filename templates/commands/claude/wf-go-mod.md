---
description: Run the Go existing-feature modification workflow
argument-hint: [workflow input]
---

# /wf-go-mod

Read `.claude/workflows/go-modify-existing.md` from the repository root and follow it as the source of truth.

Treat `$ARGUMENTS` as the workflow input.
