---
description: Run the Lark integration workflow
argument-hint: [workflow input]
---

# /wf-lark

Read `.claude/workflows/lark-integration.md` from the repository root and follow it as the source of truth.

Treat `$ARGUMENTS` as the workflow input.
