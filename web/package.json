{
  "name": "nexus-agents-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "test:unit": "tsc --module ESNext --moduleResolution Bundler --target ES2022 --strict --skipLibCheck --outDir .tmp-tests --rootDir src src/workflow-graph.ts src/project-state.ts src/types.ts && node --test tests/*.test.mjs",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vue-tsc": "^2.1.0"
  }
}
